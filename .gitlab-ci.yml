stages:
  - build

build:
  stage: build
  script:
    - docker build -t registry.gitlab.com/codesigntheory/django-base:latest ./
    - docker push registry.gitlab.com/codesigntheory/django-base:latest
  tags:
    - docker
    - build
  only:
    refs:
      - master
